---
phase: 33
plan: 1
title: "Data Integrity Blockers"
wave: 1
depends_on: []
agent: "Senior Developer"
must_haves:
  - "Sync push queries filter by active profile_id"
  - "PersonalRecordSyncDto carries prType, phase, volume"
  - "3 pre-existing PR test failures resolved"
findings: [B3, B4]
---

<objective>
Fix the two BLOCKER sync data integrity issues: cross-profile data leak in push queries (B3) and missing PR fields in the sync DTO (B4). B4 should also resolve 3 of the 4 pre-existing test failures related to PR weight/volume calculations.
</objective>

<context>
## B3: Cross-profile data leak in sync push queries
- **File:** `shared/src/commonMain/sqldelight/com/devil/phoenixproject/database/VitruvianDatabase.sq`
- **4 queries missing `AND profile_id = :profileId`:**
  - `selectSessionsModifiedSince` (line 1648): `SELECT * FROM WorkoutSession WHERE (updatedAt > ? OR updatedAt IS NULL) AND deletedAt IS NULL`
  - `selectPRsModifiedSince` (line 1686)
  - `selectRoutinesModifiedSince` (line 1695)
  - `selectCustomExercisesModifiedSince` (line 1704)
- Every other session query correctly filters by `:profileId`
- The `SqlDelightSyncRepository` methods that call these queries need to accept and pass profileId

## B4: PersonalRecordSyncDto missing critical fields
- **File:** `shared/src/commonMain/kotlin/com/devil/phoenixproject/data/sync/SyncModels.kt` (lines 97-110)
- Current fields: clientId, serverId, exerciseId, exerciseName, weight, reps, oneRepMax, achievedAt, workoutMode, deletedAt, createdAt, updatedAt
- **Missing:** `prType` (MAX_WEIGHT vs MAX_VOLUME), `phase` (COMBINED/CONCENTRIC/ECCENTRIC), `volume` (Float)
- Local PR table unique index: `(exerciseId, workoutMode, prType, phase, profile_id)`
- Without these fields, pull-merged PRs always conflict on default MAX_WEIGHT/COMBINED
- The push path through `PortalSyncAdapter` does include prType/prPhase in `PortalSetDto`, but the standalone `PersonalRecordSyncDto` used for PR-level sync does not

## Pre-existing test failures to fix
- `PersonalRecordRepositoryTest.getBestPR` — weight multiplier issue (per-cable vs display)
- `PersonalRecordRepositoryTest.getBestVolumePR` — same multiplier issue
- `ResolveRoutineWeightsUseCaseTest.respectsProgramMode` — PR mode filtering logic
- Investigate: are these test expectation errors or production code bugs?

## Key dependencies
- `SqlDelightSyncRepository` — calls the .sq queries, needs active profileId parameter
- `SyncManager.pushLocalChanges()` — calls repository methods, needs to pass profileId
- `UserProfileRepository.activeProfile` — provides the current profile ID
</context>

<tasks>
## Task 1: Add profile_id filter to sync push queries (B3)

**Action:** In `VitruvianDatabase.sq`, add `AND profile_id = :profileId` to all four sync push queries.

**Specific changes:**
1. `selectSessionsModifiedSince` (~line 1648):
   ```sql
   SELECT * FROM WorkoutSession
   WHERE (updatedAt > :lastSync OR updatedAt IS NULL)
   AND deletedAt IS NULL
   AND profile_id = :profileId;
   ```
2. `selectPRsModifiedSince` (~line 1686): add `AND profile_id = :profileId`
3. `selectRoutinesModifiedSince` (~line 1695): add `AND profile_id = :profileId`
4. `selectCustomExercisesModifiedSince` (~line 1704): add `AND profile_id = :profileId`

5. Update `SqlDelightSyncRepository` (find via grep for these query names) to:
   - Accept `profileId: String` parameter in each corresponding method
   - Pass it to the query

6. Update `SyncManager.pushLocalChanges()` to:
   - Get `activeProfileId` from `userProfileRepository.activeProfile.value?.id ?: "default"`
   - Pass it to each repository method

**Verify:** Build succeeds. Grep confirms all four queries now include `profile_id = :profileId`.

**Done when:** All sync push queries are profile-scoped.

## Task 2: Add missing fields to PersonalRecordSyncDto (B4)

**Action:** In `SyncModels.kt`, add `prType`, `phase`, and `volume` to `PersonalRecordSyncDto`.

**Specific changes:**
1. Add fields with backward-compatible defaults:
   ```kotlin
   @Serializable
   data class PersonalRecordSyncDto(
       // ... existing fields ...
       val prType: String = "MAX_WEIGHT",
       val phase: String = "COMBINED",
       val volume: Float = 0f
   )
   ```
2. Update the code that creates `PersonalRecordSyncDto` instances (in SqlDelightSyncRepository or SyncManager) to populate these fields from the local PersonalRecord table columns.
3. Update the pull-merge code that converts `PersonalRecordSyncDto` back to local records — use the compound key `(exerciseId, workoutMode, prType, phase)` for upsert matching.

**Verify:** Build succeeds. The DTO now round-trips prType/phase/volume correctly.

**Done when:** PersonalRecordSyncDto carries all fields needed for the compound unique key.

## Task 3: Investigate and fix pre-existing PR test failures

**Action:** Read the 3 failing test files and determine root cause.

**Specific changes:**
1. Read `PersonalRecordRepositoryTest` — understand what `getBestPR` and `getBestVolumePR` expect vs what they get (800 vs 400, 900 vs 450 — exactly 2x, suggesting weight multiplier)
2. Read `ResolveRoutineWeightsUseCaseTest` — understand the mode filtering test expectation
3. If the tests are wrong (expect display weight but code correctly returns per-cable): fix the test expectations
4. If the production code is wrong: fix the code
5. Verify all tests pass after fix

**Verify:** `./gradlew.bat :shared:testAndroidHostTest` — 0 failures (down from 4).

**Done when:** All PersonalRecordRepositoryTest and ResolveRoutineWeightsUseCaseTest tests pass.
</tasks>

<verification>
- `./gradlew.bat :androidApp:assembleDebug` passes
- `./gradlew.bat :shared:testAndroidHostTest` passes with 0 failures (3 pre-existing failures resolved)
- Grep: all 4 sync push queries include `profile_id = :profileId`
- Code review: PersonalRecordSyncDto has prType, phase, volume fields
- Code review: SyncManager passes activeProfileId to all sync queries
</verification>

<success_criteria>
- Multi-profile sync only pushes active profile's data (B3 resolved)
- PR records round-trip with correct prType and phase (B4 resolved)
- PersonalRecordRepositoryTest tests pass (pre-existing failures fixed)
- ResolveRoutineWeightsUseCaseTest tests pass (pre-existing failures fixed)
</success_criteria>

<output>
Plan summary for next plan's context: B3 fixed by adding profile_id filter to all 4 sync push queries. B4 fixed by adding prType/phase/volume to PersonalRecordSyncDto with compound key matching. 3 pre-existing test failures resolved.
</output>
