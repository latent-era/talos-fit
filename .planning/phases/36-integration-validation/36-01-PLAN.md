---
phase: 36
plan: 1
title: "BLE Validation"
wave: 1
depends_on: []
agent: "Senior Developer"
must_haves:
  - "Build passes with all BLE changes"
  - "All BLE-related tests pass"
  - "Code review confirms all 8 Phase 32 findings addressed"
findings: [VAL-01]
---

<objective>
Verify all Phase 32 BLE fixes work together: build compilation, test execution, and code review of the 5 modified BLE files.
</objective>

<tasks>
## Task 1: Build verification
Run `./gradlew.bat :androidApp:assembleDebug` and confirm BUILD SUCCESSFUL.

## Task 2: Test verification
Run `./gradlew.bat :shared:testAndroidHostTest` and report: total tests, failures, skipped. Confirm 0 failures (or document any remaining).

## Task 3: Code review — BLE fix completeness
Read each of these files and verify the fix is present and correct:
1. `shared/.../presentation/manager/BleConnectionManager.kt` — B1 (scannedDevices delegation) + B2 (reconnectionRequested collector)
2. `shared/.../data/ble/KableBleConnectionManager.kt` — H2 (stateObserverJob) + H3 (onDeviceReady try/catch) + H4 (scan timeout)
3. `shared/.../presentation/components/BlePermissionHandler.android.kt` — H10 (shouldShowRationale + Open Settings)
4. `shared/.../presentation/manager/ActiveSessionEngine.kt` — H1 (workoutJob try/catch) + M1 (.catch on collectors)
5. `shared/.../util/BleConstants.kt` — M2 (CONNECTION_RETRY_DELAY_MS = 1500L)

Report any missing or incorrect fixes.
</tasks>

<verification>
- assembleDebug: PASS
- testAndroidHostTest: 0 failures
- All 8 BLE findings confirmed present in code
</verification>

<output>
BLE validation result: PASS or FAIL with details.
</output>
