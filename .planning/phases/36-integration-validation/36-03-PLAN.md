---
phase: 36
plan: 3
title: "iOS Validation"
wave: 1
depends_on: []
agent: "Senior Developer"
must_haves:
  - "iOS schema version matches SQLDelight version"
  - "All columns from migrations 17-21 present in iOS manual schema"
  - "ConnectivityChecker uses NWPathMonitor"
  - "Form check gated on iOS"
  - "No Vico imports in commonMain/iosMain"
findings: [VAL-03]
---

<objective>
Verify all Phase 35 iOS fixes: schema version, column completeness, ConnectivityChecker implementation, platform gating, and dependency isolation.
</objective>

<tasks>
## Task 1: Schema version check
Read `DriverFactory.ios.kt` and verify `CURRENT_SCHEMA_VERSION = 22L` matches `build.gradle.kts` SQLDelight `version = 22`.

## Task 2: Column completeness audit
Compare the iOS manual schema columns (in `createAllTables` and `ensureAllColumnsExist`) against `VitruvianDatabase.sq` for all tables. Report any gaps.

## Task 3: ConnectivityChecker verification
Read `ConnectivityChecker.ios.kt` and verify it uses `NWPathMonitor` (not a stub returning true).

## Task 4: Platform gating
Grep for `isIosPlatform` in screen files to verify form check is gated. Grep for Vico imports in commonMain/iosMain to verify isolation.

## Task 5: Android build regression check
Run `./gradlew.bat :androidApp:assembleDebug` to confirm no regressions from iOS changes.
</tasks>

<verification>
- CURRENT_SCHEMA_VERSION = 22L
- No missing columns in iOS manual schema (vs VitruvianDatabase.sq)
- ConnectivityChecker uses NWPathMonitor
- Form check gated behind isIosPlatform
- Zero Vico imports in commonMain/iosMain
- assembleDebug: PASS
</verification>

<output>
iOS validation result: PASS or FAIL with details.
</output>
