---
phase: 36
plan: 2
title: "Sync Validation"
wave: 1
depends_on: []
agent: "Senior Developer"
must_haves:
  - "All sync-related tests pass"
  - "Profile isolation verified in sync queries"
  - "Sync DTO fields complete"
  - "Portal Edge Function uses safe upsert pattern"
findings: [VAL-02]
---

<objective>
Verify all Phase 33 sync fixes and Phase 34 token encryption work together: test execution, profile isolation check, DTO completeness, and portal Edge Function safety.
</objective>

<tasks>
## Task 1: Test verification
Run `./gradlew.bat :shared:testAndroidHostTest` and confirm 0 failures. Pay special attention to PersonalRecordRepositoryTest, PortalTokenStorageTest, and ResolveRoutineWeightsUseCaseTest (previously failing).

## Task 2: Profile isolation verification
Grep `VitruvianDatabase.sq` for all `ModifiedSince` queries and verify each has `profile_id` filter (except Exercise and Badge tables which are global by design — verify these have documentation comments).

## Task 3: Sync DTO completeness
Read `SyncModels.kt` and verify `PersonalRecordSyncDto` has: prType, phase, volume fields with defaults.

## Task 4: Portal Edge Function safety
Read `phoenix-portal/supabase/functions/mobile-sync-push/index.ts` and verify:
- routine_exercises uses upsert+orphan-cleanup (NOT delete+insert)
- cycle_days uses upsert+orphan-cleanup (NOT delete+insert)

## Task 5: Token encryption
Read `PlatformModule.android.kt` and verify EncryptedSharedPreferences is used for token storage.
</tasks>

<verification>
- testAndroidHostTest: 0 failures
- All sync push queries profile-scoped (3 of 4, with documented exceptions)
- PersonalRecordSyncDto has prType/phase/volume
- Portal uses safe upsert pattern
- Tokens encrypted
</verification>

<output>
Sync validation result: PASS or FAIL with details.
</output>
